class A {
	int beer;
};
class B {
	int okay;
	A someFunc(int lol, float more) { };
	int anotherFunc(){};
};

program {
	A foo;
	B bar;
	int cake;
	int baker;
	float aFloat;

	cake = (baker + cake) / bar.anotherFunc();
	cake = foo.beer > baker + 2 - 2 * cake;
	foo = bar.someFunc(baker == 2, aFloat);
	
	int reallyArray[3][4][7];

	cake = reallyArray[(4 - cake) * 7][reallyArray[1][baker * 2][9]][foo.beer];
	reallyArray[3 + 1][cake][bar.anotherFunc()] = baker;

};